{
  "name": "@lab/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:watch": "tsc -w"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
